import 'package:flutter/material.dart';

class ListenerFavoriteAlbumCard extends StatelessWidget {
  const ListenerFavoriteAlbumCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
